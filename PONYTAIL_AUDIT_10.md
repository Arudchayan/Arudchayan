# Ponytail Audit 10 — over-engineering findings (ranked, current tree)

- `native:` the whole banner pipeline — `banner_generator.py` (66 lines) + the Pillow dependency compositing a 800×300 PNG of the six team sprites, plus its own `urllib` downloader. The platform already renders images: the Squad Dossiers section displays every one of those same sprites via plain `<img>` tags, so the banner is a second rendering of content the README already shows. Replacement: nothing — delete the module, the `import banner_generator`, the `=== 4. BANNER GENERATION ===` block in build_readme, and the `team_banner.png` div in the template. [-1 dep: Pillow] [scripts/banner_generator.py, scripts/build_readme.py:12,519-521, README.template.md:5-7]
- `delete:` the genetics subsystem — `github_metrics.py` is now mock-only (audit 9 removed the real-fetch half), so `calculate_genetic_bonuses(get_github_stats())` derives constants from constants: level is always 86, all three bonuses always 50, desc always the same string. Two functions, a module, an import, a call+print, a 5-line stat-mutation block, two replacements-dict entries, and two template rows to produce literals. Replacement: hardcode the level/desc strings (or drop the "Genetics" rows outright). [scripts/github_metrics.py, scripts/build_readme.py:11,469-471,567-571,666-667, README.template.md:17,51]
- `delete:` `PONYTAIL_AUDIT_9.md` — stacked one-shot audit reports kept as repo content; audit 8 set the rule (keep only the newest, delete predecessors when the next lands). Replacement: this file supersedes it. [PONYTAIL_AUDIT_9.md]
- `delete:` the stray `Shiny status: Checked.` line — dead caption duplicating the `{SHINY_HUNT_STATUS}` line directly above it; carries no data. Replacement: nothing. [README.template.md:149]

Re-checked from audit 9, still clean: weather color pair lives in `WEATHER_TYPES` and is passed straight through; trainer_history holds only its two live keys; `move_cache.json` stays (485 entries, load-bearing); `__pycache__/` remains untracked. No dead functions found — every helper in build_readme/coach/svg_generator has a live caller, and all template placeholders are consumed.

net: -113 lines, -1 deps possible.
